{% extends 'base.html.twig'  %}

{% block body %}
<main class=" h-auto mb-auto">
    <section class="relative py-16 bg-gray-300 ">

        <div class="container mx-auto px-4 bg-white p-4 shadow mt-6">
            <div class="h-64 text-center">
                <h3 class="text-2xl mt-4">{{ post.title }}</h3>
                <small class="italic">Erstellt am {{ post.createdAt.format('d.m.Y H:i:s') }} von Adrian Räuchle</small>
            </div>
            <div>
                {{ post.body | raw }}

                <script src="https://gist.github.com/araeuchle/6628c25555514a75c2855106077bcd0d.js"></script>
            </div>
        </div>
    </section>


</main>

{% endblock %}
